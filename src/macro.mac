%macro _push 1-*
  %rep %0
    push %1
    %rotate 1
  %endrep
%endmacro

%macro _pop 1-*
  %rep %0
    %rotate -1
    pop %1
  %endrep
%endmacro

%macro _string 1
  [section .rodata]
  %%string: db %1
  %%string.#: equ $ - %%string

  __SECT__
  mov esi, %%string
  mov ecx, %%string.#
%endmacro
